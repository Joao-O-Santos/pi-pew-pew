image: node:22-bookworm-slim

stages:
  - validate

validate:
  stage: validate
  interruptible: true
  script:
    - npm ci
    - node scripts/install-latest-pandoc.mjs
    - npm run markdown:fix
    - cat README.md
    - exit 1
